# ──────────────────────────────────────────────────────────────────────────────
#  Tauroi Technologies — Environment Variables
# ──────────────────────────────────────────────────────────────────────────────
#  LOCAL DEVELOPMENT:
#      cp .env.example .env
#      # Fill in your credentials below
#
#  GITHUB ACTIONS:
#      Add these same variables as repository secrets:
#      Settings → Secrets and variables → Actions → New repository secret
#
#      Required secrets:
#        KALSHI_ACCESS_KEY
#        KALSHI_API_SECRET
#        CHARTMETRIC_REFRESH_TOKEN
#
#      Optional secrets (have sensible defaults):
#        CHARTMETRIC_ARTIST_ID  (default: 214945 = Bad Bunny)
#        ARTIST_NAME            (default: Bad Bunny)
#
#  DOCKER:
#      docker run --env-file .env tauroi
#
#  NEVER commit .env to version control.
# ──────────────────────────────────────────────────────────────────────────────

# Kalshi API credentials (https://kalshi.com/account/api)
KALSHI_ACCESS_KEY=your-kalshi-access-key-here
KALSHI_API_SECRET=your-kalshi-api-secret-here

# Chartmetric API (https://app.chartmetric.com → Account Settings → API)
CHARTMETRIC_REFRESH_TOKEN=your-chartmetric-refresh-token-here

# Artist configuration
# ARTIST_NAME is used for Kalshi market discovery and display.
# CHARTMETRIC_ARTIST_ID is the numeric ID for full-history calibration.
# Find the ID at: app.chartmetric.com/artist?id=<THIS_NUMBER>
ARTIST_NAME=Bad Bunny
CHARTMETRIC_ARTIST_ID=214945
