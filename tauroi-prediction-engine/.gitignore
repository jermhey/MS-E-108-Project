# ── secrets ──────────────────────────────────────────────────────────────────
.env

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/

# ── data outputs ────────────────────────────────────────────────────────────
data/processed/
data/eda/
backtest_results/

# ── IDE / OS ────────────────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store

# ── local cache ─────────────────────────────────────────────────────────────
# Cache is COMMITTED to the repo so GitHub Actions runners have fallback data
# when the Chartmetric API is rate-limited (429). Parquet files are small
# (~150KB each) and regenerated automatically when the API is available.
# cache/

# ── live trading state (positions, orders, P&L) ─────────────────────────────
# Contains runtime state that must NOT be committed — it includes
# account-specific positions and order IDs.
state/

# ── signals (track the JSONL log, ignore temp files) ────────────────────────
# signals/signal_log.jsonl is TRACKED (committed by the pipeline)
signals/*.tmp
